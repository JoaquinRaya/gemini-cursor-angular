import { Component, Output, EventEmitter } from '@angular/core';

 ...<snip>... 
    //  - Verify that the correct stream is emitted when capturing starts.
    //  - Verify that null is emitted when capturing stops.
  }
}
